{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the pet overlay window",
  "windows": ["pet-overlay"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
